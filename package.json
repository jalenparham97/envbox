{
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "envbox": "bun apps/cli/src/index.ts",
    "eb": "bun apps/cli/src/index.ts",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "latest",
    "turbo": "latest",
    "typescript": "^6.0.0"
  }
}
